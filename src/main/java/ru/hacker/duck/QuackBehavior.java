package ru.hacker.duck;

/**
 * Аналогичный интерфейс с методом quack,
 * который должен быть реализован всеми классами,
 * которые умеют крякать
 */
public interface QuackBehavior {

    void quack();
}
